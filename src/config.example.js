/* === Supabase config ===
   คัดลอกไฟล์นี้เป็น src/config.js แล้วใส่ค่าของโปรเจกต์คุณ
   - Project URL  : Supabase Dashboard → Project Settings → API → Project URL
   - anon key     : Supabase Dashboard → Project Settings → API → Project API keys → anon/public
   หมายเหตุ: anon key ปลอดภัยที่จะเปิดเผยใน browser bundle (RLS ปกป้องข้อมูลอยู่แล้ว)
*/
window.SB_URL = 'https://YOUR-PROJECT-REF.supabase.co';
window.SB_ANON_KEY = 'YOUR-ANON-PUBLIC-KEY';
