/* คัดลอกจาก config.example.js แล้วใส่ค่าจริง — ไฟล์นี้จะถูก commit เพื่อให้ Vercel deploy ได้
   anon key ปลอดภัยที่จะเปิดเผยใน browser (RLS ปกป้องข้อมูล) */
window.SB_URL = 'https://YOUR-PROJECT-REF.supabase.co';
window.SB_ANON_KEY = 'YOUR-ANON-PUBLIC-KEY';
